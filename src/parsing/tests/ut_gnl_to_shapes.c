#include "../../../includes/parsing.h"

int main(void)
{
    printf("\n");

    //printf("\n%d\n\n", gnl_to_shapes());

    printf("Finally\n");

    printf("\n");

    //* in case of stoopid
    /*
    int j = -1;
    while (++j < 3)
        printf("%d\n", j);
    */

    //* in case of stoopid
    /*
    int limit = 3;
    while (limit--)
        printf("boop\n");
    limit = 3;
    while (--limit)
        printf("nope\n");
    */
}